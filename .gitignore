config.json
.env
.claude/
.dev/
__pycache__/
*.pyc
.local/
.venv/
venv/
*.key
*.pem
*.secret
*.log

# Defense-in-depth: never commit recorded audio or meeting output, even if a
# user repoints output_dir inside the repo (default ~/Recordings is outside).
*.wav
*.mp3
/Recordings/
transkript.md
*_protokoll.md
# Slug-named protokoll, e.g. 2026-04-01_14-31_budgetplanering-q3.md.
# *_protokoll.md above only catches the fallback name; this also catches
# the normal slug names if output_dir is repointed inside the repo.
????-??-??_*.md
